@{
	var cardView = Html.DevExpress().CardView(settings => {
        settings.Name = "CardView";
        settings.CallbackRouteValues = new { Controller = "SprNaimTask", Action = "CardViewPartial" };

				settings.SettingsEditing.AddNewCardRouteValues = new { Controller = "SprNaimTask", Action = "CardViewPartialAddNew" };
        settings.SettingsEditing.UpdateCardRouteValues = new { Controller = "SprNaimTask", Action = "CardViewPartialUpdate" };
        settings.SettingsEditing.DeleteCardRouteValues = new { Controller = "SprNaimTask", Action = "CardViewPartialDelete" };
        settings.SettingsEditing.Mode = CardViewEditingMode.EditForm;
        settings.SettingsBehavior.ConfirmDelete = true;

		settings.KeyFieldName = "Id";

		settings.SettingsPager.Visible = true;
				settings.Settings.ShowHeaderPanel = true;
		settings.Settings.ShowHeaderFilterButton = true;
				settings.SettingsBehavior.AllowSelectByCardClick = true;

				settings.Columns.Add("Task");
				settings.Columns.Add("Naim_task");

				settings.CardLayoutProperties.Items.AddCommandItem(i => {
			i.ShowNewButton = true;
			i.ShowEditButton = true;
			i.ShowDeleteButton = true;
		});
		settings.CardLayoutProperties.Items.AddEditModeCommandItem();
						settings.CardLayoutProperties.Items.Add("Task");
				settings.CardLayoutProperties.Items.Add("Naim_task");
			});
		if (ViewData["EditError"] != null){
        cardView.SetEditErrorText((string)ViewData["EditError"]);
    }
}
@cardView.Bind(Model).GetHtml()