@using System.Web.UI.WebControls
@using System.Web.UI.WebControls
@using DevExpress.Web.Mvc
@{
    ViewBag.Title = "Справочник кодификаторов материала";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*<link rel="stylesheet" type="text/css" href="@Url.Content("~/Content/TableStyle.css")" />*@
<script type="text/javascript" src="@Url.Content("~/Content/GridView.js")"></script>
<h2 align="center">Справочник кодификаторов материала</h2>
@Html.DevExpress().Menu(menuSettings =>
{
    menuSettings.Name = "pageToolbar";
    menuSettings.SettingsAdaptivity.EnableCollapseToSideMenu = true;
    menuSettings.Styles.Style.CssClass = "expandedPanel";
    menuSettings.ItemAutoWidth = false;
    menuSettings.AllowSelectItem = false;
    menuSettings.ApplyItemStyleToTemplates = true;

    menuSettings.SettingsAdaptivity.Enabled = true;
    menuSettings.SettingsAdaptivity.EnableAutoHideRootItems = true;
    menuSettings.SettingsAdaptivity.EnableCollapseRootItemsToIcons = true;
    menuSettings.SettingsAdaptivity.CollapseRootItemsToIconsAtWindowInnerWidth = 600;

    menuSettings.Width = Unit.Pixel(3420);
    menuSettings.ControlStyle.SeparatorWidth = Unit.Pixel(0);
    menuSettings.ControlStyle.CssClass = "page-toolbar";

    menuSettings.Styles.Item.CssClass = "item";
    menuSettings.Styles.Item.VerticalAlign = VerticalAlign.NotSet;

    menuSettings.Images.Item.Width = Unit.Pixel(16);
    menuSettings.Images.Item.Height = Unit.Pixel(16);

    menuSettings.Items.Add(item =>
    {
        item.Text = "Вспомогательные Справочники";
        item.Image.IconID = DevExpress.Web.ASPxThemes.IconID.ActionsDownload16x16office2013;
        item.Items.Add(subItem =>
        {
            subItem.Text = "Справочник ОГТ";
            subItem.Target = "_blank";
            subItem.NavigateUrl = Url.Action("Index", "Spr_Ogt", "DirectoryOfMaterialCodifiers_default");
            subItem.Image.IconID = DevExpress.Web.ASPxThemes.IconID.XafStateTaskCompletedSvgGray16x16;
        });
        item.Items.Add(subItem =>
        {
            subItem.Text = "Цехов";
            subItem.Target = "_blank";
            subItem.NavigateUrl = "~/Spr_cex/Index";
            subItem.Image.IconID = DevExpress.Web.ASPxThemes.IconID.XafStateTaskCompletedSvgGray16x16;
        });
    });
    menuSettings.ClientSideEvents.ItemClick = "onPageToolbarItemClick";

}).GetHtml()
@*@Html.DevExpress().Panel(panel =>
    {
        panel.Name = "Panel";
        panel.Width = Unit.Percentage(100);
        panel.SetContent(() =>
        {
            Html.DevExpress().Button(s =>
            {
                s.Name = "ButtonExpand";
                s.Text = "Развернуть все вкладки";
                s.Width = Unit.Pixel(180);
                s.UseSubmitBehavior = false;
                s.ClientSideEvents.Click = "function() {gridView.ExpandAll() }";
            }).GetHtml();
        });
        Html.DevExpress().Button(s =>
        {
            s.Name = "ButtonCollapse";
            s.Text = "Свернуть все вкладки";
            s.Width = Unit.Pixel(180);
            s.UseSubmitBehavior = false;
            s.ClientSideEvents.Click = "function() {gridView.CollapseAll() }";
        }).GetHtml();
    }).GetHtml()*@

@*@Html.Action("GridView6Partial")*@

@Html.Action("GridView4Partial")
