@model Asu.Web.ViewModel.MemoAddingMaterialCodeViewModel
<div class="k-d-flex k-justify-content-center">
    <div class="k-w-600">
        @(Html.Kendo().AutoComplete()
            .Name("KodSklad")
            .DataTextField("KodSklad")
            .HtmlAttributes(new { style = "width:100%", placeholder = "Введите код склада"/*, required = "required", validationmessage = "Обязательное поле"*/ })
            //.HtmlAttributes(new { style = "width:100%" })
            .Filter(FilterType.Contains)
            .DataSource(source =>
            {
                source.Read(read =>
                {
                    read.Action("ComboBoxMemoAddingOtsRead", "Skm");
                });
            })

            )

    </div>
</div>
