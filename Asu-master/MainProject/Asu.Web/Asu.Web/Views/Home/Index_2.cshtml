@model IEnumerable<Asu.Web.Models.UsersTask.Spr_nm_task>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    //Layout = null;
}
<!DOCTYPE html>
<html>
<head>

    <link href="~/Content/bootstrap_bundle.css" rel="stylesheet" />
    <link href="~/Content/Layout_new.css" rel="stylesheet" />
    <script src="~/Content/bootstrap.js"></script>
    <meta charset="utf-8" />
</head>
<body>
    @*<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand p-0" href="">
                <img src="~/Content/Images/plane.png" alt="Logo" width="114" />
            </a>
        </div>
        <button class="navbar-toggler mx-auto" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">

            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-current="" href="">Главная</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">О нас</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">Контакты</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="">Информация</a>
                </li>
            </ul>
        </div>
    </nav>*@


    <div>
        <h3 class="title" align="center" style="margin:50px">Классификатор @TempData["UserTask"]</h3>
    </div>
    <div class="container">
        <div class="row row-cols-1 row-cols-md-3 row-cols-lg-5 g-xl-5">
            @foreach (var item in Model)
            {
                <div class="col">
                    <div class="card badge text-wrap">
                        <a href="@Url.RouteUrl(item.RouteUrl,
                                     new {controller = item.Controller_Name, action = item.Action_Name})"
                           title="@item.Title" target="_blank">
                            @Html.Raw("<img class='image-content' src=\"data:image/jpeg;base64,"
                            + Convert.ToBase64String(item.screen) + "\" />")
                            <h5 class="card-title">@item.Naim_task</h5>
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</body>

</html>

@*<div class="container">
        <div class="row row-cols-1 row-cols-md-3 row-cols-lg-5 g-xl-5">
            @foreach (var item in Model)
            {
                <div class="col">
                    <div class="card badge text-wrap">
                        <a href="@Url.RouteUrl(item.RouteUrl,
                                 new {controller = item.Controller_Name, action = item.Action_Name})"
                                 title="@item.Title" target="_blank">
                            @Html.Raw("<img class='image-content' src=\"data:image/jpeg;base64,"
                            + Convert.ToBase64String(item.screen) + "\" />")
                          <h5 class="card-title">@item.Naim_task</h5>
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>*@

